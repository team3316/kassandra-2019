const app = require('../app')
// Getting port from the enviorment or chosing the default port
const port = parseInt(process.env.PORT, 10) || 3000 
// Setting port constant
app.set('port', port)
// Listening to the port
app.listen(app.get('port'), console.log(`Kassandra listening on port ${port}`))
